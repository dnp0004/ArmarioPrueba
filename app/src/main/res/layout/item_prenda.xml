<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/cardPrenda"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    android:backgroundTint="@color/background_card"
    app:cardCornerRadius="12dp"
    app:cardElevation="4dp">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- Imagen de la Prenda -->
        <ImageView
            android:id="@+id/imgPrenda"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:scaleType="centerCrop"
            android:background="@color/gray_100"
            android:contentDescription="Imagen de prenda" />

        <!-- Overlay de Lavandería (cuando está sucia) -->
        <View
            android:id="@+id/overlayLavanderia"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/overlay_dark"
            android:visibility="gone" />

        <!-- Icono de Lavandería -->
        <ImageView
            android:id="@+id/iconoLavanderia"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_gravity="center"
            android:src="@android:drawable/ic_menu_info_details"
            android:tint="@color/text_white"
            android:visibility="gone"
            android:contentDescription="En lavandería" />

        <!-- Icono de Favorito -->
        <ImageView
            android:id="@+id/iconoFavorito"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_gravity="top|end"
            android:layout_margin="8dp"
            android:src="@android:drawable/btn_star_big_on"
            android:tint="@color/warning"
            android:visibility="gone"
            android:contentDescription="Favorito" />

        <!-- Información de la Prenda -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:background="@color/background_white"
            android:orientation="vertical"
            android:padding="12dp">

            <TextView
                android:id="@+id/txtSubtipoPrenda"
                style="@style/TextBody"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Nombre de la prenda"
                android:textStyle="bold"
                android:maxLines="1"
                android:ellipsize="end" />

            <TextView
                android:id="@+id/txtCategoriaPrenda"
                style="@style/TextCaption"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:text="Categoría"
                android:maxLines="1"
                android:ellipsize="end" />

        </LinearLayout>

    </FrameLayout>

</androidx.cardview.widget.CardView>